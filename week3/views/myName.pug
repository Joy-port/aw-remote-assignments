extends layout.pug 

block content 
    if name
        h1 Hello Welcome to #{name}'s World
        form(action='/' method='post')
            button(type='submit') clear name
    else
        form(action='/trackName' method='post')
            label
                strong Please enter your name 
                input(type='text' name='username')
            button(type='submit') Submit